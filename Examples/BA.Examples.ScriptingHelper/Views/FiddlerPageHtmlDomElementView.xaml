<UserControl x:Class="BA.Examples.ScriptingHelper.Views.FiddlerPageHtmlDomElementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" 
             d:DesignHeight="117" d:DesignWidth="511">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height=".5*" />
            <RowDefinition Height=".5*" />
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" x:Name="tbxXPath" />
        <TextBox Grid.Row="1" TextWrapping="Wrap" AcceptsReturn="True" x:Name="tbxValue" />
        <TextBox Grid.Row="2" TextWrapping="Wrap" AcceptsReturn="True" x:Name="tbxInnerText" />
    </Grid>
</UserControl>
